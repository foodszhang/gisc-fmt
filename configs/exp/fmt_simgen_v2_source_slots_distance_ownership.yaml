# @package _global_

defaults:
  - fmt_simgen_v2_source_slots_soft_union
  - _self_

name: fmt_simgen_v2_source_slots_distance_ownership
description: Ablation with distance-only ownership

model:
  source_instance_decoder:
    ownership_mode: distance
