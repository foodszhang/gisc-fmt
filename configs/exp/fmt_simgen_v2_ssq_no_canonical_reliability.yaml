# @package _global_

defaults:
  - fmt_simgen_v2_ssq_main
  - _self_

name: fmt_simgen_v2_ssq_no_canonical_reliability
description: SSQ ablation with PTFA/PCFS evidence but masked-mean view aggregation instead of canonical reliability

model:
  feature_refinement:
    enabled: true
    ptfa_view_aggregation: masked_mean
