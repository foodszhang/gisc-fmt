# @package _global_

defaults:
  - fmt_simgen_e1b_nongt_mixed
  - _self_

name: fmt_simgen_e4_residual_scorer_lambda005
description: FMT-SimGen E4 residual scorer lambda 0.05 over E1b mixed sampler

model:
  ptfa:
    enabled: false
    scales: []
  residual_scorer:
    enabled: true
    hidden_dim: 128
    lambda_r: 0.05
