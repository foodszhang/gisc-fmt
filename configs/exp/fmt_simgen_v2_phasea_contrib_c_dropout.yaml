# @package _global_

defaults:
  - fmt_simgen_v2_phasea_contrib_base
  - _self_

name: fmt_simgen_v2_phasea_contrib_c_dropout
description: Phase-A random view dropout with subset density supervision only

loss:
  view_subset_consistency:
    enabled: true
    view_counts: [3, 4, 5, 6]
    drop_mode: mixed
    density_weight: 1.0
    lambda_sub: 0.0
    support_threshold: 1.0e-6
    seed: 20260703
