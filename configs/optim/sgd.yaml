# SGD optimizer with StepLR scheduler
_target_: torch.optim.SGD
lr: 0.001
momentum: 0.9
weight_decay: 0.0001
nesterov: true
scheduler:
  _target_: torch.optim.lr_scheduler.StepLR
  step_size: 1500
  gamma: 0.1
  interval: epoch
  frequency: 1
