name: vox_dmrn
output_type: voxel
num_views: 1
in_channels: 1
feature_dim: 64
pos_enc_dim: 144
geometry:
  camera_distance: 200
  detector_size: [256, 256]
  global_voxel_shape: [190, 200, 104]
benchmark:
  display_name: Vox-DMRN (single-view adaptation)
  fidelity: architecture_proxy
  native_output_shape: [64, 64, 64]
  reference_output_shape: [190, 200, 104]
  physical_extent_mm: [38.0, 40.0, 20.8]
  table_tier: appendix
vox_dmrn:
  base_channels: 32
  num_blocks_per_stage: 2
  use_bn: true
  # The current implementation still uses a fully connected voxel head and is
  # blocked by benchmark preflight at the full reference resolution.
