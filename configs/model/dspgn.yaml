name: dspgn
output_type: voxel
num_views: 7
in_channels: 1
feature_dim: 64
pos_enc_dim: 60
geometry:
  camera_distance: 200.0
  detector_size: [256, 256]
  global_voxel_shape: [190, 200, 104]
benchmark:
  display_name: DSPGN-inspired adaptation
  fidelity: architecture_proxy
  reference_output_shape: [190, 200, 104]
  table_tier: appendix
dspgn:
  base_channels: 16
  latent_channels: 64
  internal_shape: [48, 50, 26]
  num_nodes: 64
  k_neighbors: 6
  # This is a lightweight graph proxy. It does not embed the FEM system matrix
  # or the full mesh topology used by the published DSPGN.
