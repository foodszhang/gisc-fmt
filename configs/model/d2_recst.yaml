name: d2_recst
output_type: voxel
num_views: 7
in_channels: 1
feature_dim: 64
pos_enc_dim: 60
geometry:
  camera_distance: 200.0
  detector_size: [256, 256]
  global_voxel_shape: [190, 200, 104]
benchmark:
  display_name: D2-RecST-inspired adaptation
  fidelity: architecture_proxy
  reference_output_shape: [190, 200, 104]
  physical_extent_mm: [38.0, 40.0, 20.8]
  table_tier: appendix
d2_recst:
  base_channels: 16
  latent_channels: 64
  internal_shape: [48, 50, 26]
  # The defining adversarial image-domain and perceptual transfer objectives
  # are not implemented in the current trainer; keep this appendix-only.
  lambda_perceptual: 0.0
